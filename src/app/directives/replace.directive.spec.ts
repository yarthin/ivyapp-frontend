import { ReplaceDirective } from './replace.directive';

describe('ReplaceDirective', () => {
  it('should create an instance', () => {
    const directive = new ReplaceDirective();
    expect(directive).toBeTruthy();
  });
});
